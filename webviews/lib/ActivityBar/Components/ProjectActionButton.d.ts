import { ReactNode } from "react";
export declare const ProjectActionButton: (props: {
    label: string;
    tooltip?: string;
    disabled?: boolean;
    onClick?: () => void;
    iconName?: string;
    icon?: ReactNode;
}) => JSX.Element;
