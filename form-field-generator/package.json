{
  "name": "@ballerina/form-field-generator",
  "version": "1.0.0",
  "description": "",
  "main": "lib/generate.js",
  "scripts": {
    "prepublishOnly": "npm run build",
    "prebuild": "npm run clean",
    "clean": "rimraf lib && rimraf coverage && rimraf build",
    "build": "npm run build-st && npm run build-ls && tsc --pretty",
    "watch": "tsc --pretty --watch",
    "build-st": "cd ../syntax-tree && npm run build",
    "build-ls": "cd ../lang-service && npm run build",
    "gen-fields": "npm run build && node lib/generate"
  },
  "dependencies": {
    "@ballerina/lang-service": "file:../lang-service",
    "@ballerina/syntax-tree": "file:../syntax-tree",
    "dts-generator": "^3.0.0",
    "handlebars": "^4.7.6",
    "lodash": "^4.17.11",
    "prettier": "^1.5.2",
    "vscode-uri": "^1.0.6"
  },
  "devDependencies": {
    "@types/handlebars": "^4.1.0",
    "@types/lodash": "^4.14.117",
    "copyfiles": "^2.1.0",
    "rimraf": "^3.0.2",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.5"
  },
  "author": "",
  "license": "Apache-2.0"
}
